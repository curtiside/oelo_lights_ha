# Testing dependencies for Oelo Lights Home Assistant integration
# Primary: pytest-homeassistant-custom-component
# Secondary: HA REST/WebSocket API testing tools

# Primary testing framework (first priority)
pytest-homeassistant-custom-component>=0.13.0

# Pytest and async support
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-timeout>=2.1.0

# HTTP client for REST API testing (secondary priority)
aiohttp>=3.8.0
requests>=2.31.0

# WebSocket client for HA WebSocket API (secondary priority)
websockets>=11.0

# Test utilities
pytest-mock>=3.11.0
pytest-cov>=4.1.0
